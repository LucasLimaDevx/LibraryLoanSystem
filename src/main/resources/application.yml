spring:
  application:
    name: LibraryLoanSystem

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/library_loan_system?useTimezone=true&serverTimezone=UTC
    username: root
    password: 12345678

  jpa:
    hibernate:
       ddl-auto: update
    properties:
        hibernate:
    show-sql: false
    open-in-view: false
